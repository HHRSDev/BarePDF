<UserControl x:Class="BarePDF.Views.PdfViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BarePDF.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:FindBar x:Name="FindBar" Grid.Row="0" Visibility="Collapsed"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <local:ThumbnailStrip x:Name="Thumbnails" Grid.Column="0" Visibility="Collapsed"/>
        <ListBox x:Name="PageList"
                 Grid.Column="1"
                 Background="#3A3A3A"
                 BorderThickness="0"
                 Padding="20"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 VirtualizingPanel.CacheLength="2,2"
                 VirtualizingPanel.CacheLengthUnit="Page"
                 KeyboardNavigation.DirectionalNavigation="None"
                 PreviewMouseWheel="OnPageListPreviewMouseWheel"
                 PreviewMouseLeftButtonDown="OnPageListPreviewMouseLeftButtonDown"
                 PreviewMouseMove="OnPageListPreviewMouseMove"
                 PreviewMouseLeftButtonUp="OnPageListPreviewMouseLeftButtonUp">
            <ListBox.Style>
                <Style TargetType="ListBox" BasedOn="{StaticResource {x:Type ListBox}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPanMode, RelativeSource={RelativeSource AncestorType={x:Type local:PdfViewer}}}" Value="True">
                            <Setter Property="Cursor" Value="Hand"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.Style>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,0,12"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="IsTabStop" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Visibility" Value="{Binding ItemVisibility}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White"
                            BorderBrush="#222"
                            BorderThickness="1"
                            SnapsToDevicePixels="True"
                            Width="{Binding DisplayWidth}"
                            Height="{Binding DisplayHeight}"
                            DataContextChanged="OnPageContainerDataContextChanged"
                            MouseLeftButtonDown="OnPageMouseDown"
                            MouseMove="OnPageMouseMove"
                            MouseLeftButtonUp="OnPageMouseUp">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Cursor" Value="IBeam"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPanMode, RelativeSource={RelativeSource AncestorType={x:Type local:PdfViewer}}}" Value="True">
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Image Source="{Binding Image}" Stretch="Uniform" IsHitTestVisible="False"/>
                            <local:PageFindLayer
                                MatchRects="{Binding MatchRects}"
                                CurrentMatchRects="{Binding CurrentMatchRects}"
                                IsHitTestVisible="False"/>
                            <local:PageSelectionLayer
                                SelectedRects="{Binding SelectedRects}"
                                IsHitTestVisible="False"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Border Grid.Column="2"
                Width="52"
                Background="{DynamicResource ControlSolidFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="1,0,0,0">
            <StackPanel VerticalAlignment="Top" Margin="0,12">
                <ui:Button Width="36" Height="32" Margin="0,2" Padding="0"
                           HorizontalAlignment="Center"
                           Appearance="Transparent"
                           ToolTip="First page (Ctrl+Home)"
                           Click="OnRibbonFirstPage">
                    <TextBlock Text="⏮" FontSize="14"/>
                </ui:Button>
                <ui:Button Width="36" Height="32" Margin="0,2" Padding="0"
                           HorizontalAlignment="Center"
                           Appearance="Transparent"
                           ToolTip="Previous page"
                           Click="OnRibbonPrevPage">
                    <TextBlock Text="▲" FontSize="13"/>
                </ui:Button>
                <TextBlock x:Name="PageIndicator"
                           Text="0 / 0"
                           HorizontalAlignment="Center"
                           FontSize="11"
                           Margin="0,10,0,4"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                <ui:TextBox x:Name="PageJumpBox"
                            Width="40" Height="26"
                            Padding="2,0"
                            HorizontalAlignment="Center"
                            HorizontalContentAlignment="Center"
                            PreviewKeyDown="OnPageJumpKeyDown"
                            LostFocus="OnPageJumpLostFocus"/>
                <ui:Button Width="36" Height="32" Margin="0,10,0,2" Padding="0"
                           HorizontalAlignment="Center"
                           Appearance="Transparent"
                           ToolTip="Next page"
                           Click="OnRibbonNextPage">
                    <TextBlock Text="▼" FontSize="13"/>
                </ui:Button>
                <ui:Button Width="36" Height="32" Margin="0,2" Padding="0"
                           HorizontalAlignment="Center"
                           Appearance="Transparent"
                           ToolTip="Last page (Ctrl+End)"
                           Click="OnRibbonLastPage">
                    <TextBlock Text="⏭" FontSize="14"/>
                </ui:Button>
            </StackPanel>
        </Border>
        </Grid>
    </Grid>
</UserControl>
